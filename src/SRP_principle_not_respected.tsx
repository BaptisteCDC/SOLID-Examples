class User {
    Name: string;
    Email: string;
    constructor(public name: string, public email: string) {
        this.Name = name;
        this.Email = email;
    }

    saveUser() {
        // code to save user
    }

    sendEmail() {
        // code to send email
    }
}